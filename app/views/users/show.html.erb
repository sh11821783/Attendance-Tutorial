<!-- ユーザー情報ページ -->
<!--このビューでは、埋め込みRuby（ERB）を使用してユーザーの名前とメールアドレスを表示-->
<!--@userというオブジェクトはインスタンス変数という変数のうちのひとつ-->
<% provide(:title, @user.name) %>
<div>
  <table class="table table-bordered table-condensed user-table">
    <tr>
      <td>時間管理表</td>
      <td>指定勤務時間：<%= format_basic_info(@user.work_time) %></td>
      <td>基本時間：<%= format_basic_info(@user.basic_time) %></td>
      <td>月初日</td>
    </tr>

    <tr>
      <!-- ユーザー作成後と更新後に表示される情報ページで所属の値が確認できるようにしておく。 -->
      <!-- 下記では@userのユーザーオブジェクトにdepartmentが存在するか？を確認しています。 -->
      <td>所属：<%= @user.department.present? ? @user.department : "未所属" %></td>
      <td> 名前：<%= @user.name %></td>
      <td>出勤日数</td>
      <td>月末日</td>
    </tr>
  </table>
</div>